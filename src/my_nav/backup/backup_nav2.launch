
<launch>
  <!-- 1) Static map -->
  <node pkg="map_server" type="map_server" name="map_server"
        output="screen"
        args="$(find my_nav)/maps/map.yaml" />

  <!-- 2) Static TF: map -->
  <!--   Adjust the XYZ+RPY here to wherever your robot actually starts. -->
  <node pkg="tf" type="static_transform_publisher" name="map_to_base_link"
        args="6.56 3.55 0 0 0 0 map base_link 100" />

  <!-- 3) Fake localization (optional): republishes /initialpose as TF -->
  <node pkg="amcl" type="fake_localization" name="fake_localization" output="screen">
    <param name="global_frame_id" value="map"/>
    <param name="base_frame_id"   value="base_link"/>
    <param name="odom_frame_id"   value="odom"/>
    <param name="use_map_topic"   value="true"/>  <!-- ensures it listens to /initialpose -->
  </node>

  <!-- 4) move_base -->
  <node pkg="move_base" type="move_base" name="move_base" output="screen">
    <!-- costmap params -->
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load" ns="global_costmap"/>
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load" ns="local_costmap"/>
    <rosparam file="$(find my_nav)/params/global_costmap_params.yaml" command="load"/>
    <rosparam file="$(find my_nav)/params/local_costmap_params.yaml" command="load"/>
    <!-- planner params -->
    <rosparam file="$(find my_nav)/params/dwa_local_planner_params.yaml" command="load"/>
    <!-- explicitly pick your planners -->
    <param name="base_global_planner" value="navfn/NavfnROS"/>
    <param name="base_local_planner"  value="dwa_local_planner/DWAPlannerROS"/>
  </node>

  <!-- 5) RViz -->
  <node pkg="rviz" type="rviz" name="rviz" output="screen" />
</launch>


<!--
<launch>
  <!-- Static map -->
  <node pkg="map_server" type="map_server" name="map_server"
        args="$(find my_nav)/maps/map.yaml"
        output="screen"/>

  <!-- Fake TF publisher using /initialpose -->
  <node pkg="amcl" type="fake_localization" name="
<launch>
  <!-- 1) Static map -->
  <node pkg="map_server" type="map_server" name="map_server"
        output="screen"
        args="$(find my_nav)/maps/map.yaml" />

  <!-- 2) Static TF: map -->
  <!--   Adjust the XYZ+RPY here to wherever your robot actually starts. -->
  <node pkg="tf" type="static_transform_publisher" name="map_to_base_link"
        args="6.56 3.55 0 0 0 0 map base_link 100" />

  <!-- 3) Fake localization (optional): republishes /initialpose as TF -->
  <node pkg="amcl" type="fake_localization" name="fake_localization" output="screen">
    <param name="global_frame_id" value="map"/>
    <param name="base_frame_id"   value="base_link"/>
    <param name="odom_frame_id"   value="odom"/>
    <param name="use_map_topic"   value="true"/>  <!-- ensures it listens to /initialpose -->
  </node>

  <!-- 4) move_base -->
  <node pkg="move_base" type="move_base" name="move_base" output="screen">
    <!-- costmap params -->
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load" ns="global_costmap"/>
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load" ns="local_costmap"/>
    <rosparam file="$(find my_nav)/params/global_costmap_params.yaml" command="load"/>
    <rosparam file="$(find my_nav)/params/local_costmap_params.yaml" command="load"/>
    <!-- planner params -->
    <rosparam file="$(find my_nav)/params/dwa_local_planner_params.yaml" command="load"/>
    <!-- explicitly pick your planners -->
    <param name="base_global_planner" value="navfn/NavfnROS"/>
    <param name="base_local_planner"  value="dwa_local_planner/DWAPlannerROS"/>
  </node>

  <!-- 5) RViz -->
  <node pkg="rviz" type="rviz" name="rviz" output="screen" />
</launch>
fake_localization">
    <param name="base_frame_id" value="base_link"/>
    <param name="odom_frame_id" value="odom"/>
    <param name="global_frame_id" value="map"/>
  </node>

  <!-- move_base -->
  <node pkg="move_base" type="move_base" name="move_base" output="screen">
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load"
              ns="global_costmap"/>
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load"
              ns="local_costmap"/>
    <rosparam file="$(find my_nav)/params/global_costmap_params.yaml" command="load"/>
    <rosparam file="$(find my_nav)/params/local_costmap_params.yaml" command="load"/>
    <rosparam file="$(find my_nav)/params/dwa_local_planner_params.yaml" command="load"/>

    <!-- explicitly set which plugins to use -->
    <param name="base_global_planner" value="navfn/NavfnROS"/>
    <param name="base_local_planner"  value="dwa_local_planner/DWAPlannerROS"/>
  </node>
  
  <!-- RViz -->
  <node pkg="rviz" type="rviz" name="rviz" args="-d $(find my_nav)/rviz/nav.rviz"/>

<!-- fake robot pose: map → base_link -->
<node pkg="tf" type="static_transform_publisher" name="map_to_base"
      args="6.56 3.55 0 0 0 0 map base_link 100"/>

</launch>
-->

<!--
<launch>
  <!-- Static map -->
  <node pkg="map_server" type="map_server" name="map_server"
        args="$(find my_nav)/maps/map.yaml"
        output="screen"/>

<!-- before move_base, add: -->
<!-- when use_map_topic=true, it listens on /initialpose and republishes it as map→base_link -->
<!-- <node pkg="amcl" type="fake_localization" name="fake_localization" output="screen">
  <param name="use_map_topic"  value="true"/>
  <param name="base_frame_id"  value="base_link"/>
</node> -->

  <!-- move_base -->
  <node pkg="move_base" type="move_base" name="move_base" output="screen">
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load"
              ns="global_costmap"/>
    <rosparam file="$(find my_nav)/params/costmap_common_params.yaml" command="load"
              ns="local_costmap"/>
    <rosparam file="$(find my_nav)/params/global_costmap_params.yaml" command="load"/>
    <rosparam file="$(find my_nav)/params/local_costmap_params.yaml" command="load"/>
    <rosparam file="$(find my_nav)/params/dwa_local_planner_params.yaml" command="load"/>

    <!-- explicitly set which plugins to use -->
    <param name="base_global_planner" value="navfn/NavfnROS"/>
    <param name="base_local_planner"  value="dwa_local_planner/DWAPlannerROS"/>
  </node>
  
  <!-- RViz -->
  <node pkg="rviz" type="rviz" name="rviz" args="-d $(find my_nav)/rviz/nav.rviz"/>

<!-- map → odom -->
<node pkg="tf" type="static_transform_publisher" name="map_to_odom"
      args="1.0 1.0 0 0 0 0 map odom 100"/>

<!-- odom → base_link -->
<node pkg="tf" type="static_transform_publisher" name="odom_to_base"
      args="0 0 0 0 0 0 odom base_link 100"/>

</launch>
-->
